//bit manipulation 

public class Solution {
    public int SingleNumber(int[] nums) {
        int result = 0;
        foreach (int number in nums) {
              result ^= number;
        }
    return a;
    }
}
